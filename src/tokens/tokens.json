{
  "$schema": "https://design-tokens.github.io/community-group/format/",
  "description": "Design tokens extracted from Figma — Button component (node: 8683-1477)",
  "spacing": {
    "xs": {
      "$value": "4px",
      "$type": "spacing",
      "$description": "Extra small spacing — used as gap between icon and label, and inner horizontal padding"
    },
    "sm": {
      "$value": "8px",
      "$type": "spacing",
      "$description": "Small spacing — used as vertical padding (top/bottom) of the Button"
    },
    "2xl": {
      "$value": "24px",
      "$type": "spacing",
      "$description": "2X large spacing — used as horizontal padding (left/right) of the Button"
    }
  },
  "component": {
    "button": {
      "$description": "Base layout and style tokens for the Button component",
      "layout": {
        "display": "inline-flex",
        "justifyContent": "center",
        "alignItems": "center",
        "paddingVertical": "{spacing.sm}",
        "paddingHorizontal": "{spacing.2xl}",
        "gap": "{spacing.xs}"
      },
      "css": {
        "$value": "display: inline-flex; padding: var(--sm, 8px) var(--2xl, 24px); justify-content: center; align-items: center; gap: var(--xs, 4px);",
        "$type": "string",
        "$description": "Ready-to-use CSS shorthand for the Button component"
      }
    }
  }
}
